Read `HERMES_TFT_CONTINUATION.md` first.

Current priority:
- Continue TFT Android helper training and debugging from Codex session "改进商店英雄识别".
- Use ADB screenshots/logs for real emulator state.
- Keep normal matchmaking only; never ranked.
- Use Codex MCP as the coding lane for implementation changes.

If nothing is actively running, first inspect the emulator and helper state before starting automation.

Windows command caveat: read the command notes in `HERMES_TFT_CONTINUATION.md`; use `npm.cmd` and the explicit Gradle path, not bare `npm` or missing `.\gradlew.bat`.
